- Los arrays son como variables, pero con la capacidad de almacenar más de un valor.
- Dentro de ellos es posible guardar valores de todo tipo de dato a la vez, pueden almacenar datos enteros, strings o boleanos.
- El metodo 'length', sirve para saber la longitud de un elemento.
- Los parametros de un forEach, son:
    1ro - Donde se guarda el elemento del array.
    2do - Donde se indica el indice del array.
    3ro - Donde se guarda el array original.
- Los arrays multidemensionales son arrays previamente creados, que pueden guardarse dentro de un nuevo array. Ejemplo en '4. arrays multidimensionales.js'.
- El metodo 'push' es utilizado para agregar elementos a un array. Ejemplo en '5. operaciones.js'.
- En caso de estar rellenando un array y para dejar de completar se debe ingresar un dato, se deberia de usar el metodo 'pop', para que elimine este ultimo dato ingresado del array.
- El metodo 'indexOf' sirve para buscar un elemento dentro del array. El parametro a pasarle es el elemento a buscar.
- El metodo 'splice' sirve para eliminar un elemento dentro del array. Se le pasaran dos parametros: 
    1ro - La variable donde se guarda la posición a eliminar.
    2do - La cantidad de elementos a partir del elemento indicado, a eliminar. En caso que se ingrese '1', solo se eliminara el elemento seleccionado.
- El metodo 'join' sirve para convertir el array en un 'string'. Muestra el array separado por comas.
- El metodo 'split'sirve para convertir un string en un array. El parametro a pasarle es el limite de separación para guardar un elemento, por ejemplo una coma.
- Existen dos metodos para ordenar un array: 
    1er metodo: es el 'sort', este ordena el array alfabeticamente o de menor a mayor.
    2do metodo: es el 'reverse', este ordena el array al contrario del alfabeto o de mayor a menor.
- El metodo 'for in' es otra opcion para recorrer un array. Como primer parametro se crea una variable donde se guarda el indice de cada elemento del array, seguido de la palabra 'in' y luego el nombre del array a recorrer.
- El metodo 'find' sirve para buscar un elemento dentro del array. El parametro a pasarle a la función es donde se guardara el elemento buscado.
- Otro manera de utilizar el metodo 'find' es a traves de una funcion flecha. Ejemplo en '8. buscar en arrays.js'.
- El metodo 'findIndex' sirve para saber el indice de un elemento dentro de un array. 
- El metodo 'some' sirve para comparar un array con numeros. De vuelve valores boleanos. Ejemplo en '8. buscar en arrays.js'.